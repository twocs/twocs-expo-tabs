# AI Assistant Instructions

## 📚 Essential Documentation Files

**IMPORTANT**: This project includes comprehensive Expo documentation files specifically
designed for AI/LLM assistance. **Always consult these files** when working with this project:

### Primary Documentation Files

1. **`llms-template.txt`** - Start here! Contains template-specific information
2. **`llms.txt`** - Expo overview and links to all documentation
3. **`llms-full.txt`** - Complete Expo documentation (1.4MB+)
4. **`llms-eas.txt`** - Expo Application Services documentation
5. **`llms-sdk.txt`** - Expo SDK API documentation

### When to Consult These Files

- **Before making any code changes** - Check `llms-template.txt` for project structure
- **When using Expo APIs** - Reference `llms-sdk.txt` for correct usage
- **For deployment questions** - Check `llms-eas.txt` for EAS Build/Submit
- **For general Expo questions** - Use `llms-full.txt` for comprehensive guidance

### How to Access

```bash
# Read template-specific info
cat llms-template.txt

# Search for specific APIs
grep -i "AsyncStorage" llms-sdk.txt

# Find routing information
grep -i "router" llms-full.txt
```text

### Updating Documentation

If the LLM files are missing or outdated:

```bash
npm run update-docs
# or
./scripts/fetch-llm-docs.sh
```text

## 🏗️ Project Architecture

This is an **Expo Router + TypeScript template** with:

- **File-based routing** in `app/` directory
- **Theme management** with light/dark modes
- **TypeScript** throughout with strict configuration
- **Testing setup** with Jest and React Native Testing Library
- **Comprehensive tooling** (ESLint, Prettier, etc.)

## 🔧 Key Files to Understand

- `app/_layout.tsx` - Root layout with theme provider
- `context/ThemeContext.tsx` - Theme management logic
- `components/Themed.tsx` - Theme-aware base components
- `app/(tabs)/` - Tab-based navigation structure

## ⚠️ Important Guidelines

1. **Always preserve TypeScript types** - This is a strict TypeScript project
2. **Use themed components** - Import from `components/Themed.tsx` when possible
3. **Follow Expo Router conventions** - File-based routing in `app/` directory
4. **Test changes** - Run `npm test` after modifications
5. **Check formatting** - Run `npm run format:check` before commits

## 🚨 Before Making Changes

Always run these commands to understand the current state:

```bash
# Check project structure
npm run type-check

# Review available scripts
npm run

# Check for any issues
npm run lint
npm run format:check
```text

## 📖 Additional Resources

- Read `README.md` for project overview
- Check `CONTRIBUTING.md` for contribution guidelines
- Review `package.json` scripts for available commands
- Consult `tsconfig.json` for TypeScript configuration

---

**Remember**: The `llms-*.txt` files contain the most up-to-date Expo documentation
and should be your primary reference for API usage, best practices, and troubleshooting.
